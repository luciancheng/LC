const SectionHeading = ({text}) => {
    return ( 
        <div className="sectionheading mb-20 text-5xl hidden-anim hidden-header">
            {text}
        </div>
     );
}
 
export default SectionHeading;